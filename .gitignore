# Secrets — never upload
.env

# Virtual environment — reproducible via `pip install -r requirements.txt`,
# never worth tracking (was accidentally committed early on; see CLAUDE.md)
venv/
.venv/

# Logs and backups — local only
logs/
backups/

# Local data — optional, keep private history off GitHub
data/price_logs/

# Python cache files
__pycache__/
*.pyc

# OS/editor clutter
.DS_Store
Thumbs.db